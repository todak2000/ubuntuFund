import { Instrument_Sans as FontInstrument, Sen as FontAvenir } from 'next/font/google';

export const fontAvenir = FontAvenir({
  subsets: ['latin'],
  variable: '--font-avenir',
  display: 'swap',
  adjustFontFallback: false,
});
